# 项目本地开发流程记忆

## Git 分支职责

- `main`：稳定分支，仅接收已经完成测试的代码，不直接进行日常开发。
- `dev`：开发分支，日常功能开发、修复和联调均在此分支进行。

## 开发与合并流程

1. 开始开发前切换到 `dev` 分支。
2. 每完成一个独立任务或可交付改动，先完成必要验证，然后创建一次 Git commit。
3. 同一天的开发工作完成后，在 `dev` 分支执行项目测试。
4. 只有当 `dev` 当日测试通过后，才将 `dev` 合并到 `main`。
5. 合并到 `main` 后再次确认工作区和关键测试结果，保持 `main` 可作为稳定版本使用。
6. 不要把未测试完成的改动直接合并到 `main`。

## 执行约定

- 修改前先检查当前分支和工作区状态，保留已有未提交改动。
- 提交应围绕单一完成事项，避免把无关改动混入同一个 commit。
- 本文件是项目内的流程记忆；如项目流程发生变化，应同步更新此文件。

## Commit 规则（2026-08-17 用户确认）

### 提交格式（分阶段提交）

按阶段提交，格式：`Phase X(类型)-标题`

- 标题：本次改动的一句话总结，**不超过 15 个字**。
- 示例：
  - `Phase A(test)-建立FakeLLM测试基线`
  - `Phase A(security)-CORS来源配置化`
  - `Phase B(feat)-会话隔离与消息持久化`
  - `Phase C(fix)-修复预约冲突检查`

### 类型标识

| 标识 | 含义 |
|---|---|
| feat | 新增功能 |
| fix | 修复 bug |
| docs | 文档改动 |
| refactor | 重构（不新增功能、不修 bug） |
| test | 测试相关 |
| style | 格式调整（不影响逻辑） |
| perf | 性能优化 |
| build | 构建系统/依赖改动 |
| ci | CI 配置改动 |
| chore | 杂项（配置、工具脚本） |
| revert | 回滚提交 |
| security | 安全修复 |

### 原则

- 按"大块"提交：一个阶段或一大块改动完成后才 commit，不要因小改动频繁碎 commit。
- Bug 修复单独 commit，不混入功能改动。
- 提交信息应描述该块改动的主要内容和目的，便于回溯。
